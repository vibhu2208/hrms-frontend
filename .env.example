# API Configuration
# Production backend URL (used when building for production)
VITE_API_URL=https://hrms-backend-xbz8.onrender.com

# Development Setup:
# - During development (npm run dev), Vite proxy forwards /api requests to localhost:5000
# - No .env file needed for development - uses proxy configuration in vite.config.js
# - For production builds, set VITE_API_URL in your deployment platform

# For Vercel deployment:
# 1. Set VITE_API_URL in Vercel dashboard under Project Settings > Environment Variables
# 2. Use the production URL: https://hrms-backend-xbz8.onrender.com
